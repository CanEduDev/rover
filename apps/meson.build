# Populated in subdirs
app_binaries = []
app_elfs = []

# Common app linker script
app_linker_script = fs.copyfile(files('app.ld'))

subdir('battery-monitor')
subdir('light-array')
subdir('obstacle-detector')
subdir('sbus-receiver')
subdir('servo')
subdir('wheel')
